#! /bin/sh
# /etc/init.d/light_requester_starter

# You need to register your script to run at boot
# sudo update-rc.d light_requester_starter defaults

# To remove it from boot:
# sudo update-rc.d -f light_requester_starter remove

# Carry out specific functions when asked to by the system
case "$1" in
  start)
    echo "Starting light_requester.py"
    nohup /media/2/code/raspberry_adc/light_requester.py --log /media/2/log/light_requester.log &
    ;;
  stop)
    echo "Stopping light_requester.py"
    killall light_requester.py
    ;;
  restart)
    echo "Stopping light_requester.py"
    killall light_requester.py
    sleep 2
    echo "Starting light_requester.py"
    nohup /media/2/code/raspberry_adc/light_requester.py --log /media/2/log/light_requester.log &
    ;;
  *)
    echo "Usage: /etc/init.d/light_requester_starter {start|stop|restart}"
    exit 1
    ;;
esac

exit 0
